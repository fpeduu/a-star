from data import distances, real_distances, get_cost

# for station in (real_distances):
#     for key in real_distances[station]:
#         print(f'[{station}][{key}]: {real_distances[station][key]}')

# * implement the A* algorithm
